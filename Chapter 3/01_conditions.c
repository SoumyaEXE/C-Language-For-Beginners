#include <stdio.h>

int main(){
    int age = 10;
    if (age > 9){
    printf("He's %d Years Old, Go To School!\n", age);
    } if (age%5==0) {
    printf("Your age is divisible by 5!\n");
    }
    return 0;
}