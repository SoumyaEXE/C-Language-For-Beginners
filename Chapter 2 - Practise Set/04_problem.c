#include <stdio.h>

int main(){
    //  3*x/y – z+k, where x=2, y=3, z=3, k=1
    int x = 2, y = 3, z = 3, k =1;
    float e = 3*x/y - z+k;
    printf("The Value Of The Given is : %d", e);
    // 3*x/y – z+k
    // 3*2/3 - z+k
    // 6/3 - z+k
    // 2 - z+k
    // -1 + 1
    // 0
    return 0;
}